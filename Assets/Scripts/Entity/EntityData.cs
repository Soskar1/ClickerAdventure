public struct EntityData
{
    public string name;
    public float maxHealth;

    public EntityData(string name, float maxHealth)
    {
        this.name = name;
        this.maxHealth = maxHealth;
    }
}
